# Python dependencies (for the sticker_pack_export_pipeline.py script)
# JSX scripts require no installation — they run natively in Adobe apps

Pillow>=8.0.0
pywin32>=300
reportlab>=3.5.0
opencv-python>=4.5.0
numpy>=1.19.0
